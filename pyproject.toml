[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "mnist-bnn-v2"
version = "0.1.0"
dependencies = [
    "numpy",
    "Pillow",
    "pyyaml"
]

[tool.setuptools.packages.find]
where = ["."]
include = ["sw*", "config*"]
